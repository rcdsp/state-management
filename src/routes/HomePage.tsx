import { FC } from 'react';
import { Container } from '@mui/material';

const HomePage: FC = () => {
  return (
    <Container maxWidth="xl">
      <h1>Welcome!</h1>
    </Container>
  );
};

export default HomePage;
